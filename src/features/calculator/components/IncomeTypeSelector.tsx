/**
 * Phase 5B — IncomeTypeSelector
 *
 * Two-tap toggle between `salary` (給与所得) and `business` (事業所得).
 *
 * TODO(Phase 5B):
 * - Segmented control style, Apple HIG height ~32, theme.colors.brandSubtle
 *   background, active segment in theme.colors.surface with elevation
 * - Reads/writes `useCalculatorStore(s => s.category)`
 * - Two labels: t('calculator.types.salary') + t('calculator.types.business')
 * - Helper subtitle under each (e.g. "Seishain, baito" vs "Freelance, 個人事業")
 * - Switching wipes prefecture/municipality state to force re-pick
 */

export function IncomeTypeSelector() {
  return null;
}
