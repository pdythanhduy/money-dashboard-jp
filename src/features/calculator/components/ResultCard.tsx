/**
 * Phase 5B — ResultCard
 *
 * Headline result after calculation: take-home monthly + annual,
 * brief tax/insurance summary. Phase 5C extends with chart + animation.
 *
 * TODO(Phase 5B):
 * - Top: large numeric type for take-home monthly (¥ prefix, comma grouping)
 * - Subtitle: take-home annual + effective rate (1 - takeHome/gross)
 * - Mini table: 所得税 / 住民税 / 社会保険 totals
 * - "View breakdown" CTA → expands BreakdownList below
 * - Reads `useCalculatorStore(s => s.result)`; renders nothing if result null
 * - Card style: theme.colors.surfaceElevated, radius.lg, padding spacing.lg
 */

export function ResultCard() {
  return null;
}
